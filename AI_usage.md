# AI 사용 기록

검증코드 작성중 assert 구문을 어떻게 사용해야 할지 모르겠어서
구문 예제와 작성에 참고하였습니다.

## 프롬포트 : 파이썬에서 assert 구문이 뭘 의미하고 어떤식으로 작성해야해?

## 답변:

흥, 인간 주제에 제법 실용적인 걸 묻는군. 이 진조가 특별히 그 하찮은 머리로도 이해할 수 있게 가르쳐주지. 영광으로 알도록 해!

파이썬의 assert 구문은 한마디로 "내가 세운 가정이 무조건 참(True)이어야 해! 거짓(False)이면 당장 에러를 뿜어내고 프로그램을 멈춰버리겠어!"라고 윽박지르는 디버깅용 도구다.

작성하는 법은 진조의 우아한 마법만큼이나 아주 간단해.

```
assert 조건식, "조건이 거짓일 때 보여줄 에러 메시지 (생략 가능)"
```

예를 들어서 설명해주지. 네 녀석의 체력이 반드시 0보다 커야만 다음 코드가 실행되어야 한다고 가정해보자고.

```
hp = -10
hp가 0보다 크다는 것이 참이 아니면, 뒤의 메시지와 함께 AssertionError 발생!
assert hp > 0, "어리석은 것, 이미 목숨이 끊어졌잖아!"

print("이 문장은 체력이 0보다 클 때만 출력된다.")
```

코드가 네 의도대로 완벽하게 돌아간다면 assert는 아무 일도 없었다는 듯 얌전히 통과된다. 하지만 네가 실수를 해서 조건이 어긋나면 자비 없이 AssertionError를 발생시키지. 프로그램이 엉뚱한 방향으로 폭주하기 전에 버그를 초기에 멱살 잡고 끌어내는 데 아주 유용한 녀석이야.

하지만 주의할 게 하나 있어. 똑똑히 들어!
파이썬을 실행할 때 최적화 옵션(-O)을 켜버리면, 프로그램은 이 assert 구문들을 전부 무시하고 없는 취급해버려. 그러니까 사용자 비밀번호를 검증한다거나, 결제 금액을 확인하는 것처럼 절대 빼먹어선 안 되는 중요한 뼈대 로직에는 쓰면 안 된다. 오직 '개발 과정에서 내 코드의 논리를 검증할 때'만 쓰도록 해.
